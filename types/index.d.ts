// Description: This file contains the types for the application.
// The types are used to define the shape of the data that will be sent between the frontend and the backend.

export type CharacterResponse = {
  text: string;
};

export type UserComment = {
  text: string;
};
